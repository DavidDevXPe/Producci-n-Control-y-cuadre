import type { PerformanceBenchmarkConfig } from './types'

/**
 * Benchmarks remain intentionally empty until Operations validates a target.
 * The calculation layer already supports defaults by process and overrides by shift.
 */
export const PERFORMANCE_BENCHMARKS: readonly PerformanceBenchmarkConfig[] = []
